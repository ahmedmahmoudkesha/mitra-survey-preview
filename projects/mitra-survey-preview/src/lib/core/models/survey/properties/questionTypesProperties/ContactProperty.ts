import {BaseProperty} from '../BaseProperty';

export class ContactProperty extends BaseProperty {
    first_name = true;
    last_name = true;
    address = true;
    phone_number = true;
    email = true;
}
